const menuBar = document.querySelector(menu_bar)
const ulResponsive = document.querySelector(ul-responsive)

menubar.addEventListener("click", () =>{
menuresponsive.classList.toggle("menu-responsive:hover")
})